spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:yapp}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
    out-of-order: false

logging:
  level:
    org.flywaydb: info

jasypt:
  encryptor:
    password: ${JASYPT_PASSWORD}

app:
  auth:
    tokenSecret: 9a1c7805bb074f31f9b36fa510d0c743a557aa44f9b5b1cd6dbb7f8905b067c8
    tokenExpiry: 7776000000 #3?

oauth:
  kakao:
    clientSecret: ENC(1HUUOTxg+nG/WC8QYY+YViuX4UcOcuErIX7e3+lE/zaEl9XX9Ov7wBvtTO65++sm)
  apple:
    key: test